封装和解封装：



封装：每一层都需要给原始数据添加一些对于这一层很重要的数据，就是加一层包裹（应用层到数据链路层）

解封装：拆快递



应用层：封装说明取决于应用协议本身

传输层：端口号——TCP/UDP协议

网络层：IP地址——IP协议

数据链路层：MAC地址——以太网协议

物理层

![image-20260708212003486](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260708212003486.png)



![image-20260716113920497](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260716113920497.png)

 





数据链路层

**以太网二型帧**

![image-20260708103112945](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260708103112945.png)

Data是原始数据包裹，左边的叫头部，右边的叫尾部

左起第一个是前导符，一个标记点，方便对数据进行截断（确定每个数据帧开头的东西）

左起第二个和第三个分别为目标MAC地址和源MAC地址

左起第四个为类型，是上层所使用的协议类型，方便得知在解封装时需要将数据交给上层的哪个协议

右起第一个是帧校验序列，确保数据完整性，用的CRC循环冗余算法，对前面的数据进行计算算出一个值，发送前发送后两次结果不一致就直接丢弃







传输层

TCP/UDP

1、TCP协议是面向连接的协议，UDP是无连接的协议；《扔球游戏》在正式发送数据之前，先建立一个点到点的连接

​	**TCP建立连接的方式：三次握手**	

​		A->B：SYN=1，序列号=X

​		B->A：ACK=1，SYN=1，确认序列号=X+1（一旦携带数据就是一个字节加一，并不是单纯+1），序列号=Y

​		A->B：ACK=1，确认序列号=Y+1，序列号=X+1

​	**TCP断开连接的方式：四次挥手**

​		A->B：FIN=1，序列号=X

​		B->A：ACK=1，确认序列号=X+1，序列号=Y（为什么不顺便给A发FIN呢，因为B可能还有东西没发完）

​		B->A：FIN=1，序列号=Y

​		A->B：ACK=1，确认序列号Y+1，序列号=X+1（知道为什么断开会话之后还能发这个ACK吗，因为这里面不包含数据，属于信令，信令无需连接就可发送，就比如三次握手第一步也不会有数据，也属于信令）

2、TCP协议的传输是可靠的（确认机制，重传机制：不回ACK就重发，排序机制，流控机制），UDP的传输“尽力而为”；

3、TCP协议可以实现流控，UDP不行；

​	一次性多发几次数据包，对方一次确认（滑动窗口说是🤔）

​	A->B：序号=1，窗口=3

​	A->B：序号=2，窗口=3

​	A->B：序号=3，窗口=3

​	B->A：确认序号=4，序号=Y，窗口=3

​	······（如果三个窗口接收的游刃有余，后面就可能加窗口数）

​	······（如果处理不掉，A也能从B的确认序列号看出最多能处理多少）

4、TCP协议可以分段，UDP不行；（有序号可以拼回去）

5、TCP协议传输速度较慢，占用资源较大；UDP传输速度较快，占用资源少。

TCP和UDP的应用场景---TCP适用于对可靠性要求较高，对效率要求较低的场景；UDP适用于对可靠性要求较低，对效率要求较高的场景。







**TCP：**（传输控制协议）

![image-20260708114728328](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260708114728328.png)

序列号就是球上画的数字，确认序列号是接球的人回复给扔球的人下一个该扔什么

首部长度：因为选项长度不固定，会影响整个头部的长度，所以需要记录头部的整体长度（头部长度不固定）

保留：暂时没用

保留右边是六个开关，是1则生效，0则不生效（开关作用：······）

​	URG：紧急标记位，可以优先处理一部分数据（16为紧急指针指向紧急数据的位置，当然，得置为1时才会被激活）

​	ACK：确认标记位，接球的人给扔球的人发的确认信息时会置为1（确认序列号才会被激活）

​	PSH：置为1，不需要在缓存区等待，直接推给进程处理

​	RST：强制中断，发送了这个数据就直接中断

​	SYN：请求标记位，扔球的人给接球的人传时置为1

​	FIN：准备断开连接时置为1

窗口大小和流控有关

校验和（目的和上面的CRC算法一致）





**UDP：**（用户数据报文协议）

![image-20260708143457857](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260708143457857.png)







 

网络层：

IP协议：（IPv4）

![image-20260708210642736](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260708210642736.png)

16位总长度-4位首部长度=数据长度

TTL---数据每经过路由器的一次转发，TTL会减1，当一个路由器收到TTL为0的数据包，则将不转发该数据，直接丢弃。（就是在整个路由网络中被转发的次数）

协议——上层所使用的协议类型---TCP-6，UDP---17





MTU：最大传输字节1550字节

​	因为有了这个最大限制，所以需要对数据进行分段分片，以防数据丢失

MSS---最大段长度---理论值1460（TCP三次握手时会协商，如果两边的MSS值不一样，则按照MSS值较小的来执行。）



所以16位标识——3位标志——13位片偏移这三个用来分片的东西，以防之前用的是UDP没分段

16位标识---所有同一个数据包的分片，标识相同

3位标志：

​	第一位：保留数据，无用

​	第二位：Don't fragment---置1代表没有分片，0代表分片

​	第三位：More fragments -置1代表还有更多片，0代表是最后一片或者没有分片

13位片偏移：

​	300，400，300

​	片偏移：0，300，700

​	













HTTP：



通过HTTP协议访问web服务器

​	1，TCP的三次握手

​	2，之后发送HTTP请求报文
​		GET————直接获取网页信息

​		POST————提交信息
3，WEB服务器回复HTTP应答报文

​	200访问成功

​	301，302--重定向

​	404网页故障

​	501，502-服务器故障







| 分类       | 首位数字 | 核心含义                   | 典型场景                         |
| ---------- | -------- | -------------------------- | -------------------------------- |
| 信息响应   | 1xx      | 请求已接收，需继续处理     | 服务器确认收到请求，等待后续数据 |
| 成功响应   | 2xx      | 请求已成功接收，理解并处理 | 页面正常加载，数据请求成功       |
| 重定向响应 | 3xx      | 请求需进一步跳转才能完成   | 网页搬家、URL简化、登录后跳转    |
| 客户端错误 | 4xx      | 客户端请求存在错误         | 输入错误URL、未登录访问受限页面  |
| 服务器错误 | 5xx      | 服务器无法处理合法请求     | 服务器宕机、代码报错、资源耗尽   |











**TCP/IP的跨层封装**
(1）两种模型之间的不同点:
	TCP/IP支持跨层封装，而OSI不行
	OSI参考模型的核心思想是分层，而分层的目的就是上层协议在其下层协议提供的服务的基础上提供增值服务。所以，OSI在设计协议的时候，层次之间还是存在依赖性的；
	TCP/IP模型其本身就是先有的协议，后有的模型。TCP/IP协议簇里的协议本身都是相互独立的，每层中的协议可以根据系统的需要进行组合匹配。

![image-20260801114409331](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260801114409331.png)

(2）跨层封装的目的
	提高封装和解封装的速度，加快传输效率。



(3）跨层封装的应用

​	TCP/IP的跨层封装一般应用在直连设备之间的通讯。一般有两种形式

​	跨第四层封装，应用层封装完，跳过传输层，直接去下一层封装

​	特点：直连路由设备之间（传输层的功能由下层去实现，分片代替分段，protocol代替端口号，就是那8位协议）

​	典型代表：OSPF、ICMP



​		传输层：端口号，分段网络层：IP，分片
​			正常封装，其上层是TCP或者UDP协议。TCP协议对应的协议号是6，UDP协议对应的协议号是17。但是我们这个协议号的取值范围是0-255(8位二进制)剩余的这些协议号都是用来标定跨层封装协议的。比如我们OSPF协议，对应的协议号是89。ICMP协议，对应协议号是1





跨三四层封装、

​	应用层封装完，跳过传输层，网络层，直接去下一层封装

特点：直连交换设备之间

典型代表：

抓取STP数据包



传输层：端口号，分段

网络层：IP寻址、分片



三四层的工作需要二层完成，以太网Ⅱ帧里类型字段，可以用来区分上层协议，勉强能完成四层工作，但是三层的分片工作并无法完成。这时候我们就需要使用另外一种以太网的帧结构了，802.3帧。

![image-20260801141431011](C:\Users\xgz24\AppData\Roaming\Typora\typora-user-images\image-20260801141431011.png)

LLC子层（逻辑链路控制子层）：为传输可靠性提供一个保障，减少出现帧丢失、重复、失序。负责分片和提供帧类型号

MAC子层（媒体接入控制子层）：负责识别网络层协议，然后对它们进行数据帧的封装/解封装，MAC地址寻址，差错检验，负责正常的MAC地址和前







三、访问一个谷歌（百度）服务器的流程?



1、主机需要一个IP地址才能上网（本场景中通过DHCP服务获取IP地址)

2、首先要得到 www.goole.com(www.baidu.com)的ip地址

3、客户端知道谷歌（百度）的ip地址后，会触发客户端与服务器建立TCP连接（TCP三次握手过程）

4、建立连接后，客户端使用http协议发送数据包给服务器

5、当所有数据都接受完毕后，取消连接(TCP的四次挥手)
